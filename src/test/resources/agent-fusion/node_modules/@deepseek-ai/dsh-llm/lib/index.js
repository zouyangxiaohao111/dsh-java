/**
 * @deepseek-ai/dsh-llm overlay root.
 *
 * Honest overlay: the full real package (packages/llm/llm) wraps a ~950-line
 * LlmRuntime adapter registry whose value deps pull @deepseek-ai/schemastery
 * and a peer chain (dsh-attachment / dsh-brand / dsh-invariants / dsh-timeout
 * / cordis) — out of scope for the agent-fusion proof. What the proof
 * requires is the real message surface (message construction), the real
 * chunk→message assembler (BlockAssembler), the real error taxonomy
 * (HarnessError / LlmError / errorChain) and the request-marking/freeze
 * utilities — all type-stripped from the real sources. Requiring the package
 * resolves; a future overlay can widen this to the full LlmRuntime.
 *
 * @module @deepseek-ai/dsh-llm
 */

export { MessageId, CallId, ProviderRequestId, ReasoningEffortId } from './brand.js'
export {
  CONTEXT_SUMMARY_MAX_CHARS,
  boundContextSummary,
  freezeMessage,
  createMessage,
  createUserMessage,
  createAssistantMessage,
  createToolResultMessage,
  isTokenDelta,
} from './message.js'
export { callConfigEquals, deepFreeze, isAgentLoopRequest, markAgentLoopRequest } from './call-config.js'
export { HarnessError, LlmError, errorChain, isHarnessError } from './error.js'
export { assertNever } from './never.js'
export { BlockAssembler } from './assembler.js'
