/**
 * Overlay of the type-only `types.ts` from @deepseek-ai/dsh-session-projection
 * (SessionProjectionMap). Type-stripped to nothing — the real file declares
 * only interfaces.
 *
 * @module @deepseek-ai/dsh-session-projection/types
 */
export {}
