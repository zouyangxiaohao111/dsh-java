/**
 * Overlay of the type-only `types.ts` from @deepseek-ai/dsh-session-stats
 * (SessionStatsProjection). Type-stripped to nothing — the real file declares
 * only types.
 *
 * @module @deepseek-ai/dsh-session-stats/types
 */
export {}
