var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name2 in all)
    __defProp(target, name2, { get: all[name2], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// koishi/plugins/common/echo/src/index.ts
var src_exports = {};
__export(src_exports, {
  Config: () => Config,
  apply: () => apply,
  name: () => name,
  parsePlatform: () => parsePlatform
});
module.exports = __toCommonJS(src_exports);
var import_koishi = require("koishi");

// koishi/plugins/common/echo/src/locales/zh-CN.yml
var zh_CN_default = { commands: { echo: { description: "发送消息", options: { escape: "发送转义消息", unescape: "发送反转义消息", user: "发送到用户", channel: "发送到频道", guild: "指定群组编号" }, messages: { "expect-text": "请输入要发送的文本。", "platform-not-found": "找不到指定的平台。" } } } };

// koishi/plugins/common/echo/src/index.ts
function parsePlatform(target) {
  const index = target.indexOf(":");
  const platform = target.slice(0, index);
  const id = target.slice(index + 1);
  return [platform, id];
}
__name(parsePlatform, "parsePlatform");
var name = "echo";
var Config = import_koishi.Schema.object({});
function apply(ctx, config) {
  ctx.i18n.define("zh-CN", zh_CN_default);
  ctx.command("echo <message:text>").option("escape", "-e", { authority: 3 }).option("unescape", "-E", { authority: 3 }).option("user", "-u [user:user]", { authority: 3 }).option("channel", "-c [channel:channel]", { authority: 3 }).option("guild", "-g [guild:string]", { authority: 3 }).action(async ({ options, session }, message) => {
    if (!message)
      return session.text(".expect-text");
    let content = [message];
    if (options.unescape) {
      content = import_koishi.h.parse(message);
    } else if (options.escape) {
      content = [import_koishi.h.escape(message)];
    }
    const target = options.user || options.channel;
    if (target) {
      const [platform, id] = parsePlatform(target);
      const bot = ctx.bots.find((bot2) => bot2.platform === platform);
      if (!bot) {
        return session.text(".platform-not-found");
      } else if (options.user) {
        await bot.sendPrivateMessage(id, content, session.guildId);
      } else {
        await bot.sendMessage(id, content, options.guild);
      }
      return;
    }
    return content;
  });
}
__name(apply, "apply");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  Config,
  apply,
  name,
  parsePlatform
});
//# sourceMappingURL=index.js.map
